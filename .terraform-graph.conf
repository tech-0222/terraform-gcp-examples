# terraform graph 共通設定（Basic-Examples/* / Advanced-Examples/* の DEPENDENCY-GRAPH.svg 生成用）
# scripts/generate-terraform-graphs.sh が読み込む
# GitHub Actions は使わない。ローカルで scripts/generate-terraform-graphs.sh を実行する。

OUTPUT_FILE=DEPENDENCY-GRAPH.svg
RANKDIR=LR
NODESEP=0.4
RANKSEP=0.8
SPLINES=polyline
SHAPE=box
FONTSIZE=10
ARROW_SIZE=0.7
