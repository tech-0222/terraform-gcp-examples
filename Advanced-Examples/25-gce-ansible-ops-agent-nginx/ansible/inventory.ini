# The playbook runs on the VM itself (ansible-playbook -c local), so the only
# host is localhost. Keeping an inventory file anyway means the same playbook
# can later be driven from a control node without changes.
[web]
localhost ansible_connection=local
