# Applied manually from the bastion (the cluster has no path reachable by
# Terraform's kubernetes provider). The cloud.google.com/neg annotation is
# what makes GKE create the neg-app-a Network Endpoint Group that ilb.tf's
# backend service references by name. This NEG must exist before
# `terraform apply -var="enable_ilb=true"` -- wait 1-2 minutes after
# applying this manifest, since NEG creation is asynchronous.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-a
  namespace: default
  labels:
    app: app-a
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-a
  template:
    metadata:
      labels:
        app: app-a
    spec:
      containers:
        - name: app-a
          image: hashicorp/http-echo:0.2.3
          args:
            - "-listen=:8080"
            - "-text=backend-a"
          ports:
            - containerPort: 8080
          resources:
            requests:
              memory: "16Mi"
              cpu: "10m"
            limits:
              memory: "32Mi"
              cpu: "50m"
---
apiVersion: v1
kind: Service
metadata:
  name: app-a
  namespace: default
  annotations:
    cloud.google.com/neg: '{"exposed_ports":{"8080":{"name":"neg-app-a"}}}'
spec:
  type: ClusterIP
  selector:
    app: app-a
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
